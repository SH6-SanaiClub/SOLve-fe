이 폴더는 홈 메인 대시보드 상단 캐릭터 PNG용 경로입니다.

사용 경로:

- `/images/home/main-mascot.png`

설명:

- `src/pages/home/HomePage.tsx`에서 위 경로를 직접 사용합니다.
- 현재 레포에는 원본 캐릭터 PNG가 없어서, 실제 파일을 이 이름으로 넣으면 바로 화면에 반영됩니다.
- 정적 경로를 쓴 이유는 원본 파일이 아직 없는 상태에서도 빌드가 깨지지 않게 하기 위해서입니다.
